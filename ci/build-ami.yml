---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}

inputs:
- name: centos-packer
- name: node-exporter-release

run:
  path: ./centos-packer/ci/scripts/ami-build

