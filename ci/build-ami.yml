---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}

inputs:
- name: packer-centos
- name: node-exporter-release

run:
  path: ./packer-centos/ci/scripts/ami-build

